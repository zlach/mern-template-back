import { Router } from 'express';
// import UserController from '../controllers/user.controller.js';

const route = Router();

// route.route('/:email')
//     .get(UserController.getOneByEmail);
// route.route('/')
//     .post(UserController.create);

export default route;
